source {{ full_path }}/{{ venv }}/{{ package_name }}/bin/activate
_OLD_PYTHONPATH=$PYTHONPATH
exit_env () {
    export PYTHONPATH="$_OLD_PYTHONPATH"
    unset _OLD_PYTHONPATH
    unset -f exit_env
    deactivate
}
PYTHONPATH={{ core4_home }}:$PYTHONPATH
PATH={{ python_path }}:$PATH
